[build]
builder = "dockerfile"
dockerfilePath = "backend-rs/Dockerfile"

[deploy]
startCommand = "/usr/local/bin/backend-rs"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
